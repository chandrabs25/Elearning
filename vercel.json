{
  "buildCommand": "cd elearning-platform && npm install && npm run build",
  "outputDirectory": "elearning-platform/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.py"
    }
  ]
}
